1.Display name concatenated with dept no separated by comma and space. Name the column as ‘Emp info’ 
2.Display the emp details who do not have manager. 
3.Write a query which will display name, department no and date of joining of   all employees joined between  January 1, 1981 and March 31, 1983. Sort it based on date of   joining (ascending).
4.Write a query to list all employees and their email.To generate email use 2   to 5  characters from ename .Concat it with 2 to 4 characters in job and   then concat it   with ‘@mycompany.com’
5.Display all employees who joined between years 1981 to 1983.
6.Create following tables for given schema.(We will be using this table
  for further transactions as well).
  trainer(tid PK, tname, cid fk)
  course(cid pk, cname)
   1. insert 5 meaningful records in both the tables.
   2. In the above course table , add new column fees.
7. delete all employees who are not working in department 30 and 20.
8. Using table alias display eno,ename,deptno and dname .
9. change the job to manager and mgr to 7777 for all employees in deptno 10.
10. Display hiredate from emp table in different formats. 
11. Select all employees along with dnamae for sal > 20000.